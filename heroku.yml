setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE
build:
  docker:
    web: Dockerfile
    # migration: Dockerfile
  config:
    PORT: '3000'
    NODE_ENV: production
# release:
#   command:
#     - node db-migrate.js
#   image: migration
run:
  web: npm start